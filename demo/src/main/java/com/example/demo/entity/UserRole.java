package com.example.demo.entity;

/**
 * UserRole Enum: Defines the two roles in the system
 * 
 * ROLE_ADMIN: Admin user with special privileges
 * ROLE_USER: Regular student user
 */
public enum UserRole {
    ROLE_ADMIN,
    ROLE_USER
}
